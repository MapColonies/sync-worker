---
# Source: sync-worker/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: asdfasdfasdfasdf-sync-worker
  labels:
    app: sync-worker
    component: sync-worker
    environment: development
    release: asdfasdfasdfasdf
    helm.sh/chart: sync-worker-1.4.0
    app.kubernetes.io/name: sync-worker
    app.kubernetes.io/instance: asdfasdfasdfasdf
    app.kubernetes.io/version: "1.4.0"
    app.kubernetes.io/managed-by: Helm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sync-worker
      release: asdfasdfasdfasdf
      run: asdfasdfasdfasdf-sync-worker
      app.kubernetes.io/name: sync-worker
      app.kubernetes.io/instance: asdfasdfasdfasdf
  template:
    metadata:
      labels:
        app: sync-worker
        release: asdfasdfasdfasdf
        run: asdfasdfasdfasdf-sync-worker
        app.kubernetes.io/name: sync-worker
        app.kubernetes.io/instance: asdfasdfasdfasdf
    spec:
      volumes:
      - name: sources-storage
        persistentVolumeClaim:
          claimName: internal-pvc
      - name: key-file
        configMap:
          name: asdfasdfasdfasdf-sync-worker-configmap
          items:
          - key: "PRIVATE_KEY"
            path: "private_key.pem"
      containers:
        - name: asdfasdfasdfasdf-sync-worker-deployment
          image: my-registry/sync-worker:latest
          env:
            - name: SERVER_PORT
              value: "8089"
          envFrom:
            - configMapRef:
                name: asdfasdfasdfasdf-sync-worker-configmap
          ports:
            - name: http
              containerPort: 8089
              protocol: TCP
          livenessProbe:
            initialDelaySeconds: 60
            httpGet:
              path: /liveness
              port: http
          volumeMounts: 
            - name: sources-storage
              mountPath: /layerSources
              subPath: tiles
            - mountPath: "/usr/src/app/private_key.pem"
              name: key-file
              subPath: private_key.pem
